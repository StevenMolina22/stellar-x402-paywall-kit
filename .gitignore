node_modules/
.env
dist/
*.log
.data/

# Build output. Sources are .ts; these are emitted in place by `npm run build`
# so package.json's main/exports/bin/files can keep pointing at the same paths.
src/*.js
bin/*.js
